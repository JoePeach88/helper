## 1.3.0 (2026-09-09) [stable]

### Global updates

* Added multilanguage support
* Translated to Russian (only returned data, not debug messages)
* Bug fixes for venv

### Core module

* Added support for multilang lib
* Translated to Russian
* Fixed bug with relative path when trying to install core update
* Fixed bug with incorrect f-string in ru_RU.lng file
* Fixed bug with update from dev to stable release

### Modules module

* Method `test` renamed to `start` and now it locates in `test` subclass
* Added new method `viewreport` to `test` subclass
* Added support for multilang lib
* Translated to Russian
* `rendermd` compatible with localization, now it renders docstring from lang file or `__doc__` attribute
* New subclass `language` with `tranlsate` method
